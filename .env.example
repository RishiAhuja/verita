# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/verita_dev

# Customer API keys are stored hashed; this is only for ops/internal routes
OPS_API_KEY=dev-ops-key-change-me

# Webhook signature verification (HMAC-SHA256)
WEBHOOK_SIGNING_SECRET=dev-webhook-secret-change-me

# App
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000
